# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Streamlit — ignore the whole directory, not just secrets.toml. Secrets have
# a way of ending up in config.toml by mistake, and that file is not secret
# by default.
.streamlit/

# Claude Code tooling config (local, not part of the app)
.claude/
