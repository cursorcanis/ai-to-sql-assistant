# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Streamlit — secrets.toml only. config.toml carries the theme and MUST be
# committed or Streamlit Cloud won't apply it. Never put credentials in it:
# unlike secrets.toml it is a tracked, public file.
.streamlit/secrets.toml

# Claude Code tooling config (local, not part of the app)
.claude/
